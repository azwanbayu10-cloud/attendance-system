import { useState } from 'react'; import { api } from '../api/client';
export default function Register(){ const [form,setForm]=useState({}); return <form className="card mx-auto max-w-xl space-y-3" onSubmit={async e=>{e.preventDefault(); await api.post('/auth/register', form); alert('Registered');}}><h1 className="text-2xl font-bold">Employee Self Registration</h1>{['employeeNo','firstName','lastName','email','phone','password'].map(k=><input key={k} className="input" type={k==='password'?'password':'text'} placeholder={k} onChange={e=>setForm({...form,[k]:e.target.value})}/>) }<button className="btn">Register</button></form>; }
