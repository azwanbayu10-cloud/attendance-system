import { useEffect, useState } from 'react'; import { api } from '../../api/client'; import MetricCard from '../../components/MetricCard';
export default function AdminDashboard(){ const [data,setData]=useState({}); useEffect(()=>{api.get('/admin/dashboard').then(r=>setData(r.data));},[]); return <section className="space-y-6"><h1 className="text-3xl font-bold">Admin Dashboard</h1><div className="grid gap-4 md:grid-cols-3"><MetricCard label="Employees" value={data.employees || 0}/><MetricCard label="Departments" value={data.departments || 0}/><MetricCard label="Today" value={data.todayAttendances || 0}/></div><div className="card"><h2 className="font-bold">Modules</h2><p>Employee, department, position, schedule, QR generator, monitoring, reports, and settings are available through the API and ready for extension.</p><a className="mt-4 inline-flex rounded-xl bg-blue-600 px-4 py-2 font-semibold text-white" href="/admin/modules">Open Nama Modul</a></div></section>; }
